const DEFAULT_LESSONS=[
{id:'l1',icon:'📄',title:'HTML Dasar',desc:'Struktur website dari nol',level:'Pemula',read:10,content:'<h2>HTML Dasar</h2><p>HTML (HyperText Markup Language) adalah bahasa dasar untuk membuat website.</p><h3>Struktur Dasar</h3><pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n  &lt;title&gt;Judul&lt;/title&gt;\n&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;Halo Dunia&lt;/h1&gt;\n  &lt;p&gt;Paragraf pertama&lt;/p&gt;\n&lt;/body&gt;\n&lt;/html&gt;</code></pre><h3>Tag Penting</h3><ul><li><b>h1-h6</b> — heading</li><li><b>p</b> — paragraf</li><li><b>a</b> — link</li><li><b>img</b> — gambar</li><li><b>div</b> — container</li></ul>'},
{id:'l2',icon:'🎨',title:'CSS Dasar',desc:'Styling website jadi cantik',level:'Pemula',read:12,content:'<h2>CSS Dasar</h2><p>CSS (Cascading Style Sheets) untuk mempercantik HTML.</p><h3>3 Cara Pakai CSS</h3><ol><li>Inline: &lt;div style="color:red"&gt;</li><li>Internal: &lt;style&gt; div { color: red; } &lt;/style&gt;</li><li>External: &lt;link rel="stylesheet" href="style.css"&gt;</li></ol><h3>Selector</h3><pre><code>p { color: blue; }          /* tag */\n.box { padding: 10px; }      /* class */\n#header { background: red; } /* id */</code></pre><h3>Box Model</h3><p>Setiap elemen punya: content → padding → border → margin.</p>'},
{id:'l3',icon:'⚡',title:'JavaScript Dasar',desc:'Bikin website interaktif',level:'Pemula',read:15,content:'<h2>JavaScript Dasar</h2><p>JavaScript bikin website hidup — tombol jalan, data berubah.</p><h3>Variabel</h3><pre><code>let nama = "Yadi";\nconst umur = 25;\nvar kota = "Jakarta";</code></pre><h3>Fungsi</h3><pre><code>function sapa(nama) {\n  return "Halo " + nama;\n}</code></pre><h3>Kondisi & Loop</h3><pre><code>if (umur >= 18) { /* dewasa */ }\nfor (let i = 0; i < 5; i++) { }</code></pre>'},
{id:'l4',icon:'☕',title:'Java Dasar',desc:'Bahasa OOP paling populer',level:'Pemula',read:15,content:'<h2>Java Dasar</h2><p>Java adalah bahasa OOP yang dipakai Android, backend, dan enterprise.</p><h3>Hello World</h3><pre><code>public class Main {\n  public static void main(String[] args) {\n    System.out.println("Hello");\n  }\n}</code></pre><h3>Variabel</h3><pre><code>int umur = 25;\nString nama = "Yadi";\nboolean aktif = true;</code></pre>'},
{id:'l5',icon:'🐍',title:'Python Dasar',desc:'Bahasa paling mudah untuk pemula',level:'Pemula',read:12,content:'<h2>Python Dasar</h2><p>Python = bahasa paling ramah pemula. Sintaksnya mirip bahasa Inggris.</p><pre><code># Variabel\nnama = "Yadi"\numur = 25\n\n# Fungsi\ndef sapa(nama):\n    return f"Halo {nama}"\n\n# Loop\nfor i in range(5):\n    print(i)</code></pre>'},
{id:'l6',icon:'🎯',title:'CSS Flexbox',desc:'Layout modern yang powerful',level:'Menengah',read:14,content:'<h2>CSS Flexbox</h2><p>Flexbox = cara paling gampang bikin layout.</p><pre><code>.container {\n  display: flex;\n  justify-content: center; /* horizontal */\n  align-items: center;     /* vertikal */\n  gap: 20px;\n}</code></pre><h3>Properti Penting</h3><ul><li>justify-content: flex-start | center | space-between</li><li>align-items: flex-start | center | stretch</li><li>flex-direction: row | column</li></ul>'},
{id:'l7',icon:'📱',title:'Android Activity',desc:'Siklus hidup Activity',level:'Menengah',read:14,content:'<h2>Android Activity</h2><p>Activity = satu layar di app Android.</p><h3>Siklus Hidup</h3><pre>onCreate → onStart → onResume\n↓\nonPause → onStop → onDestroy</pre><h3>Intent</h3><pre><code>Intent i = new Intent(this, MainActivity.class);\nstartActivity(i);</code></pre>'},
{id:'l8',icon:'🟣',title:'Kotlin Dasar',desc:'Bahasa modern Android',level:'Menengah',read:13,content:'<h2>Kotlin Dasar</h2><p>Kotlin = bahasa resmi Android sejak 2017.</p><pre><code>val nama = "Yadi"     // immutable\nvar umur = 25        // mutable\n\nfun tambah(a: Int, b: Int): Int = a + b</code></pre><h3>Null Safety</h3><pre><code>var x: String? = null\nx?.length  // aman</code></pre>'},
{id:'l9',icon:'🌐',title:'WebView Android',desc:'Tampilkan web di app',level:'Menengah',read:10,content:'<h2>WebView</h2><p>WebView = browser mini di dalam app.</p><pre><code>WebView wv = new WebView(this);\nwv.getSettings().setJavaScriptEnabled(true);\nwv.loadUrl("https://example.com");</code></pre><p>⚠️ Jangan lupa permission INTERNET!</p>'},
{id:'l10',icon:'🐙',title:'Git & Version Control',desc:'Track perubahan kode',level:'Menengah',read:14,content:'<h2>Git</h2><p>Git = sistem version control untuk track perubahan kode.</p><pre><code>git init\ngit add .\ngit commit -m "pesan"\ngit push origin main</code></pre><h3>Branch</h3><pre><code>git checkout -b fitur-baru\ngit merge fitur-baru</code></pre>'},
{id:'l11',icon:'🔐',title:'Keamanan Aplikasi',desc:'Keystore, permission, signing',level:'Lanjutan',read:15,content:'<h2>Keamanan App</h2><h3>Keystore</h3><p>Buat sekali, simpan selamanya. Kalau hilang = tidak bisa update app.</p><pre><code>keytool -genkey -v -keystore key.jks ...</code></pre><h3>Runtime Permission (Android 6+)</h3><pre><code>if (checkSelfPermission(...) != GRANTED) {\n  requestPermissions(...);\n}</code></pre>'},
{id:'l12',icon:'🚀',title:'Optimasi Performa',desc:'Bikin app ngebut',level:'Lanjutan',read:12,content:'<h2>Optimasi</h2><ul><li>Kerja berat di background thread</li><li>RecyclerView > ListView</li><li>Cache gambar (Glide/Coil)</li><li>Kurangi overdraw</li><li>Proguard/R8</li></ul>'},
{id:'l13',icon:'🎨',title:'React Dasar',desc:'Library UI populer',level:'Menengah',read:16,content:'<h2>React</h2><pre><code>function Hello({nama}) {\n  return &lt;h1&gt;Halo, {nama}!&lt;/h1&gt;;\n}\n\nfunction App() {\n  return &lt;Hello nama="Yadi" /&gt;;\n}</code></pre><h3>State</h3><pre><code>const [count, setCount] = useState(0);\n&lt;button onClick={() =&gt; setCount(count+1)}&gt;{count}&lt;/button&gt;</code></pre>'},
{id:'l14',icon:'💾',title:'Database SQL',desc:'Simpan data terstruktur',level:'Menengah',read:15,content:'<h2>SQL Dasar</h2><pre><code>CREATE TABLE user (\n  id INT PRIMARY KEY,\n  nama VARCHAR(100),\n  umur INT\n);\n\nSELECT * FROM user WHERE umur &gt; 18;\nINSERT INTO user VALUES (1, \'Yadi\', 25);\nUPDATE user SET umur = 26 WHERE id = 1;\nDELETE FROM user WHERE id = 1;</code></pre>'},
{id:'l15',icon:'🔌',title:'API & REST',desc:'Komunikasi antar aplikasi',level:'Menengah',read:14,content:'<h2>REST API</h2><p>API = cara aplikasi ngobrol satu sama lain.</p><h3>HTTP Methods</h3><ul><li><b>GET</b> — ambil data</li><li><b>POST</b> — kirim data baru</li><li><b>PUT</b> — update</li><li><b>DELETE</b> — hapus</li></ul><pre><code>fetch("https://api.example.com/users")\n  .then(r =&gt; r.json())\n  .then(data =&gt; console.log(data));</code></pre>'},
{id:'l16',icon:'🐳',title:'Docker Dasar',desc:'Container untuk development',level:'Lanjutan',read:15,content:'<h2>Docker</h2><p>Docker = container yang bikin app jalan konsisten di mana aja.</p><pre><code>FROM node:20-alpine\nWORKDIR /app\nCOPY . .\nRUN npm install\nCMD ["npm", "start"]</code></pre><pre><code>docker build -t myapp .\ndocker run -p 8080:80 myapp</code></pre>'},
{id:'l17',icon:'⚛️',title:'Vue.js Dasar',desc:'Framework JS progresif',level:'Menengah',read:13,content:'<h2>Vue.js</h2><pre><code>&lt;template&gt;\n  &lt;h1&gt;{{ pesan }}&lt;/h1&gt;\n&lt;/template&gt;\n&lt;script&gt;\nexport default {\n  data() { return { pesan: "Halo" } }\n}\n&lt;/script&gt;</code></pre>'},
{id:'l18',icon:'🅰️',title:'Angular Dasar',desc:'Framework full-featured',level:'Lanjutan',read:15,content:'<h2>Angular</h2><pre><code>@Component({\n  selector: "app-root",\n  template: "&lt;h1&gt;{{title}}&lt;/h1&gt;"\n})\nexport class AppComponent {\n  title = "YadCode";\n}</code></pre>'},
{id:'l19',icon:'💚',title:'Node.js Backend',desc:'JavaScript di server',level:'Menengah',read:14,content:'<h2>Node.js</h2><pre><code>const express = require("express");\nconst app = express();\n\napp.get("/", (req, res) =&gt; {\n  res.send("Hello World");\n});\n\napp.listen(3000);</code></pre>'},
{id:'l20',icon:'🔥',title:'Error Handling',desc:'Cara handle error dengan baik',level:'Menengah',read:12,content:'<h2>Error Handling</h2><pre><code>try {\n  // kode berisiko\n  let x = JSON.parse(data);\n} catch (e) {\n  console.error("Error:", e.message);\n} finally {\n  // selalu jalan\n}</code></pre><h3>Jenis Error</h3><ul><li>Syntax Error</li><li>Runtime Error</li><li>Logic Error</li></ul>'},
{id:'l21',icon:'📦',title:'Array & Object',desc:'Struktur data dasar',level:'Pemula',read:11,content:'<h2>Array & Object</h2><pre><code>// Array\nlet buah = ["apel", "mangga", "jeruk"];\nbuah.push("pisang");\nconsole.log(buah[0]); // apel\n\n// Object\nlet user = {\n  nama: "Yadi",\n  umur: 25,\n  sapa: function() { return "Halo"; }\n};</code></pre>'},
{id:'l22',icon:'🔄',title:'Loop & Iteration',desc:'Ulangi tugas otomatis',level:'Pemula',read:10,content:'<h2>Loop</h2><pre><code>// For\nfor (let i = 0; i &lt; 5; i++) {\n  console.log(i);\n}\n\n// While\nlet x = 0;\nwhile (x &lt; 5) { x++; }\n\n// ForEach (array)\nbuah.forEach(item =&gt; console.log(item));</code></pre>'},
{id:'l23',icon:'🎯',title:'Function & Scope',desc:'Blok kode yang reusable',level:'Pemula',read:12,content:'<h2>Function</h2><pre><code>// Regular\nfunction tambah(a, b) { return a + b; }\n\n// Arrow\nconst kali = (a, b) =&gt; a * b;\n\n// Default param\nfunction sapa(nama = "Yadi") {\n  return "Halo " + nama;\n}</code></pre>'},
{id:'l24',icon:'🎪',title:'Event & DOM',desc:'Interaksi user',level:'Pemula',read:13,content:'<h2>DOM & Event</h2><pre><code>// Ambil elemen\nconst btn = document.querySelector("#myBtn");\n\n// Event listener\nbtn.addEventListener("click", () =&gt; {\n  alert("Diklik!");\n});\n\n// Ubah konten\ndocument.getElementById("title").textContent = "Baru";</code></pre>'},
{id:'l25',icon:'🎨',title:'CSS Grid',desc:'Layout 2 dimensi',level:'Menengah',read:13,content:'<h2>CSS Grid</h2><pre><code>.grid {\n  display: grid;\n  grid-template-columns: repeat(3, 1fr);\n  gap: 20px;\n}</code></pre><p>Grid cocok untuk layout kompleks 2D. Flexbox untuk 1D.</p>'},
{id:'l26',icon:'🌈',title:'CSS Animation',desc:'Bikin web hidup',level:'Menengah',read:14,content:'<h2>CSS Animation</h2><pre><code>@keyframes bounce {\n  0%, 100% { transform: translateY(0); }\n  50% { transform: translateY(-20px); }\n}\n\n.box {\n  animation: bounce 1s infinite;\n}</code></pre>'},
{id:'l27',icon:'📱',title:'Responsive Design',desc:'Web enak dilihat di semua device',level:'Menengah',read:12,content:'<h2>Responsive</h2><pre><code>@media (max-width: 768px) {\n  .container { flex-direction: column; }\n}</code></pre><h3>Meta Viewport</h3><pre><code>&lt;meta name="viewport" content="width=device-width,initial-scale=1"&gt;</code></pre>'},
{id:'l28',icon:'⚡',title:'Async JavaScript',desc:'Promise & async/await',level:'Lanjutan',read:15,content:'<h2>Async</h2><pre><code>// Promise\nfetch(url).then(r =&gt; r.json()).then(data =&gt; {});\n\n// Async/await\nasync function load() {\n  const r = await fetch(url);\n  const data = await r.json();\n}</code></pre>'},
{id:'l29',icon:'🧪',title:'Testing Dasar',desc:'Pastikan kode berjalan benar',level:'Lanjutan',read:13,content:'<h2>Testing</h2><pre><code>function tambah(a, b) { return a + b; }\n\ntest("tambah 2+3 = 5", () =&gt; {\n  expect(tambah(2, 3)).toBe(5);\n});</code></pre>'},
{id:'l30',icon:'🔍',title:'Debugging',desc:'Cari & fix bug',level:'Pemula',read:11,content:'<h2>Debugging</h2><ul><li><b>console.log()</b> — print value</li><li><b>breakpoint</b> — pause execution</li><li><b>debugger</b> — statement untuk pause</li></ul><pre><code>console.log("Nilai x:", x);</code></pre>'},
{id:'l31',icon:'📊',title:'Algoritma Sorting',desc:'Urutkan data',level:'Lanjutan',read:14,content:'<h2>Sorting</h2><pre><code>// Bubble Sort\nfunction bubble(arr) {\n  for (let i = 0; i &lt; arr.length; i++)\n    for (let j = 0; j &lt; arr.length-1; j++)\n      if (arr[j] &gt; arr[j+1]) [arr[j],arr[j+1]] = [arr[j+1],arr[j]];\n  return arr;\n}</code></pre>'},
{id:'l32',icon:'🔎',title:'Algoritma Search',desc:'Cari data efisien',level:'Lanjutan',read:12,content:'<h2>Searching</h2><pre><code>// Binary Search (array harus sorted)\nfunction binary(arr, target) {\n  let l = 0, r = arr.length - 1;\n  while (l &lt;= r) {\n    const m = Math.floor((l + r) / 2);\n    if (arr[m] === target) return m;\n    if (arr[m] &lt; target) l = m + 1;\n    else r = m - 1;\n  }\n  return -1;\n}</code></pre>'},
{id:'l33',icon:'🧬',title:'Rekursi',desc:'Fungsi panggil diri sendiri',level:'Lanjutan',read:13,content:'<h2>Rekursi</h2><pre><code>function faktorial(n) {\n  if (n &lt;= 1) return 1;\n  return n * faktorial(n - 1);\n}\n\n// faktorial(5) = 5*4*3*2*1 = 120</code></pre>'},
{id:'l34',icon:'🌳',title:'Struktur Data Tree',desc:'Hierarki data',level:'Lanjutan',read:15,content:'<h2>Tree</h2><pre><code>class Node {\n  constructor(val) {\n    this.val = val;\n    this.left = null;\n    this.right = null;\n  }\n}</code></pre><p>Tree dipakai di: file system, DOM, database index.</p>'},
{id:'l35',icon:'📚',title:'Stack & Queue',desc:'Struktur data LIFO & FIFO',level:'Menengah',read:12,content:'<h2>Stack & Queue</h2><p><b>Stack</b> — LIFO (Last In First Out). Contoh: undo, back button.</p><p><b>Queue</b> — FIFO (First In First Out). Contoh: printer, task scheduler.</p><pre><code>let stack = [];\nstack.push(1);  // masuk\nstack.pop();   // keluar (1)</code></pre>'},
{id:'l36',icon:'🎯',title:'Big O Notation',desc:'Ukur efisiensi algoritma',level:'Lanjutan',read:14,content:'<h2>Big O</h2><ul><li><b>O(1)</b> — konstan (terbaik)</li><li><b>O(log n)</b> — binary search</li><li><b>O(n)</b> — linear search</li><li><b>O(n log n)</b> — merge sort</li><li><b>O(n²)</b> — bubble sort</li></ul>'},
{id:'l37',icon:'🎨',title:'UI/UX Design',desc:'Prinsip desain',level:'Menengah',read:13,content:'<h2>UI/UX</h2><ol><li><b>Konsisten</b> — warna & font sama</li><li><b>Hierarki</b> — elemen penting menonjol</li><li><b>Feedback</b> — tombol kasih respon</li><li><b>Minimalis</b> — buang yang tidak perlu</li><li><b>Aksesibilitas</b> — kontras & font cukup</li></ol>'},
{id:'l38',icon:'📱',title:'Android Intent',desc:'Komunikasi antar Activity',level:'Menengah',read:12,content:'<h2>Intent</h2><pre><code>// Kirim data\nIntent i = new Intent(this, DetailActivity.class);\ni.putExtra("nama", "Yadi");\nstartActivity(i);\n\n// Terima\nString nama = getIntent().getStringExtra("nama");</code></pre>'},
{id:'l39',icon:'🎯',title:'Android RecyclerView',desc:'List data efisien',level:'Menengah',read:14,content:'<h2>RecyclerView</h2><pre><code>RecyclerView rv = findViewById(R.id.rv);\nrv.setLayoutManager(new LinearLayoutManager(this));\nrv.setAdapter(new MyAdapter(data));</code></pre><p>Lebih efisien dari ListView karena recycle view.</p>'},
{id:'l40',icon:'🔔',title:'Notifikasi Android',desc:'Kirim notifikasi ke user',level:'Menengah',read:12,content:'<h2>Notifikasi</h2><pre><code>NotificationCompat.Builder b = new NotificationCompat.Builder(this, CH)\n  .setContentTitle("YadCode")\n  .setContentText("Selamat belajar!")\n  .setSmallIcon(R.drawable.ic_notif);\nNotificationManagerCompat.from(this).notify(1, b.build());</code></pre>'},
{id:'l41',icon:'🌐',title:'HTTP & Web',desc:'Cara kerja web',level:'Pemula',read:13,content:'<h2>HTTP</h2><p>HTTP = protokol komunikasi web.</p><h3>Request Methods</h3><ul><li>GET, POST, PUT, DELETE</li></ul><h3>Status Codes</h3><ul><li>200 OK</li><li>404 Not Found</li><li>500 Server Error</li></ul>'},
{id:'l42',icon:'🔒',title:'HTTPS & Security',desc:'Amankan komunikasi',level:'Lanjutan',read:12,content:'<h2>HTTPS</h2><p>HTTPS = HTTP + enkripsi SSL/TLS.</p><ul><li>Data terenkripsi</li><li>Server terverifikasi</li><li>Integritas data terjaga</li></ul><p>Selalu pakai HTTPS untuk data sensitif!</p>'}
];
function ensureLessons(){if(!DB.get('lessons',null))DB.set('lessons',DEFAULT_LESSONS);return DB.get('lessons',[])}
function renderLessons(){const grid=document.getElementById('lessonGrid');if(!grid)return;let lessons=ensureLessons();const q=(document.getElementById('searchLearn')?.value||'').toLowerCase();const lvl=document.getElementById('filterLevel')?.value||'';if(q)lessons=lessons.filter(l=>l.title.toLowerCase().includes(q)||l.desc.toLowerCase().includes(q));if(lvl)lessons=lessons.filter(l=>l.level===lvl);if(lessons.length===0){grid.innerHTML='<p style="color:var(--text-muted);text-align:center;padding:40px">Tidak ada hasil.</p>';return}grid.innerHTML=lessons.map(l=>'<div class="lesson-card" onclick="openLesson(\''+l.id+'\')"><div class="lesson-icon">'+l.icon+'</div><h3>'+l.title+'</h3><p>'+l.desc+'</p><div class="lesson-meta"><span>📖 '+l.read+' min</span><span>'+l.level+'</span></div></div>').join('')}
function openLesson(id){const l=ensureLessons().find(x=>x.id===id);if(!l)return;trackView('lesson',id);document.getElementById('lessonContent').innerHTML='<div class="reader-content"><h1>'+l.title+'</h1><p class="meta">📖 '+l.read+' min • '+l.level+'</p><div class="body">'+l.content+'</div><div style="margin-top:24px;text-align:center"><a href="quiz.html" class="btn btn-primary">🎯 Ikuti Quiz</a> <button class="btn btn-secondary" onclick="closeLesson()">Tutup</button></div></div>';document.getElementById('lessonModal').classList.add('open')}
function closeLesson(){document.getElementById('lessonModal').classList.remove('open')}
document.addEventListener('DOMContentLoaded',renderLessons);
